## 1. Update Source Files

- [x] 1.1 Change `(def port 9000)` to `(def port 9365)` in `boot.janet`
- [x] 1.2 Update doc comment referencing port 9000 in `src/main.rs`

## 2. Verify

- [x] 2.1 Build the project with `just build` to confirm no errors
- [x] 2.2 Run tests with `just test` to confirm nothing is broken
