## 1. Workflow Update

- [x] 1.1 Add `echo '<meta http-equiv="refresh" content="0; url=latest/janet-api.html">' > _site/index.html` to the "Prepare docs for deployment" step in `.github/workflows/release.yml`, after the existing `mkdir`/`cp` commands
