## 1. Camera model

- [x] 1.1 Add `dolly()` method to `OrbitCamera` in `src/viewer/camera.rs` that translates `target` along `forward()`

## 2. Input wiring

- [x] 2.1 Wire Shift+scroll to dolly in `app.rs` MouseWheel handler
- [x] 2.2 Wire Shift+RMB drag to dolly in `app.rs` CursorMoved handler

## 3. Documentation

- [x] 3.1 Add Shift+Scroll and Shift+RMB entries to help overlay in `help.rs`

## 4. Build & verify

- [x] 4.1 Build and lint: `just lint` and `just fmt-check`
